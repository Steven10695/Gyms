@model List<Gym>

<h1>Lista de Gimnasios registrados</h1>

@if (Model.Count == 0) {
    <div class="alert alert-warning text-center">Aún no hay gimnasios.</div>
} else {
    <ol>
    @foreach (var p in Model) {
        <li class="d-block">
            <div>@p.Nombre </div>
            <div> @p.Ciudad</div>
        </li>
    }
    </ol>
}